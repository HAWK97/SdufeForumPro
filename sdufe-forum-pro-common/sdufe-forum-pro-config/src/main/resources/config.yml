spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${sdufe.forum.nacos.server.url}
      config:
        server-addr: ${sdufe.forum.nacos.server.url}
        file-extension: properties
        name: ${spring.application.name}